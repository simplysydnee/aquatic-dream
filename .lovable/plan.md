## Plan

1. **Restore the Search Console connector credentials**
   - The project shows the Search Console connection as linked, but gateway calls return `Credential not found`.
   - Ask for a reconnect of the existing `Aquatic Dreams Search Console` connection so the project gets fresh usable credentials.

2. **Verify Search Console access after reconnect**
   - Confirm the connector credentials work through the gateway.
   - List Search Console sites to see whether `https://aquaticdreamsswim.com/` is already verified.

3. **Complete the remaining Search Console setup**
   - If the domain is not verified, use the META verification flow.
   - If a meta verification tag is needed, add it to `index.html`, then verify once the live site has it.
   - Add/confirm the verified site in Search Console.

4. **Submit the sitemap**
   - Submit `https://aquaticdreamsswim.com/sitemap.xml` for the verified property.
   - Mark the remaining SEO finding fixed once verification and sitemap submission are confirmed.